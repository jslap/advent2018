{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "command": "make my_exe",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}